#include "wclock.ceu"
#include "usart.ceu"

spawn Usart(9600);

loop do
    var[20] byte str = [].."Hello World!\n";
    par/and do
        await Usart_TX(&str);
    with
        await Usart_TX(&str);
    end
    await 1s;
end
